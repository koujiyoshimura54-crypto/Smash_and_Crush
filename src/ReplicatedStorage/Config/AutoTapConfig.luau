-- Input cadence only; Strength multipliers and automatic combat timing are unchanged.
return table.freeze({
 AutoInterval=0.5,
 ManualTapCooldown=0.25,
 ManualAttackCooldown=0.25,
 ManualAttackAnimationSpeed=2.0,
})
