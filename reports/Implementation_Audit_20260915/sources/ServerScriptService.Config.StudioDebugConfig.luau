-- Server-only Studio debugging. Stop Play, change the flag, then start Play again.
-- Production servers ignore this configuration, even when true.
return {
 GamePassEffectsEnabled = false,
 ForceSpeedPremiumUnowned = false,
}

